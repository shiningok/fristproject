server.port=8888

# DB 설정 # jdbc:mysql:// http:// ftp:// smtp://
spring.datasource.url=jdbc:mysql://192.168.0.56/first_project
spring.datasource.username=root
spring.datasource.password=1234
# Thymeleaf (View, 출력화면) 설정
spring.thymeleaf.enabled=true
spring.thymeleaf.cache=false
# application.properties 입장에서의 소속 패키지(폴더 경로)
# classpath: (/src/main/resources)
spring.thymeleaf.prefix=classpath:/templates/views/
spring.thymeleaf.suffix=.html
spring.jpa.show-sql=true

# 파일 전송 사용 여부
spring.servlet.multipart.enabled=true
# MultipartFile 최대 크기
spring.servlet.multipart.max-file-size=100MB
# MultipartFile과 데이터를 포함한 전체 크기
spring.servlet.multipart.max-request-size=110MB
# spring.servlet.multipart.location=D:\\Students/home/location

file.image.pickuparea=/images/pickuparea
file.image.store=/images/store
file.image.food=/images/food